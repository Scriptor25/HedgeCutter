void printf: char* format, ...

int fib: int n = {
    if n < 0 break

    result n == 0
        ? 0
        : n == 1
        ? 1
        : fib(n - 1) + fib(n - 2)
} = 0

int main: int argc, char** argv = {
    int n = 10
    int f = fib(n)

    printf("Hello World!")
    printf("Fib of {} = {}", n, f)

    result f
}
